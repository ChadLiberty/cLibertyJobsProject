def retrieve_data(job_listings):
    # should the method that retrieves the data from the web
    # assert that you get more than 100
    assert job_listings > 200
    retrieve_data()

def save_file():
    #  save file contains job
    # assert that the save file, jobListing.txt from main will have a job listing, "Site Reliability Engineer"
    save_file()